export function calculateProjection(inputs) {
  // 🔁 Adapted from Zaster-Modern
  // No DOM, no document.getElementById
  // Pure calculation

  return {
    timeline,        // year-by-year rows
    netWorthEnd,
    netWorthStart,
    totalSavings,
    fire: {
      fireAge,
      fireNumber
    }
  }
}
